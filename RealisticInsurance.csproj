<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>RealisticInsurance</AssemblyName>
    <RootNamespace>RealisticInsurance</RootNamespace>
    <Version>0.0.91</Version>
    <CopyLocalLockFileAssemblies>false</CopyLocalLockFileAssemblies>
    <DeployToSPT Condition="'$(DeployToSPT)' == ''">false</DeployToSPT>
  </PropertyGroup>

  <!--
    Compiled against the assemblies shipped in the install rather than the
    SPTarkov.* NuGet packages: NuGet is still on 4.1.2 and 4.1.3 changed
    server APIs.

      dotnet build -c Release -p:SPTPath="H:\SPT4.1.X"
  -->
  <PropertyGroup>
    <SPTPath Condition="'$(SPTPath)' == ''">$(SPT_PATH)</SPTPath>
    <SPTPath Condition="'$(SPTPath)' != ''">$(SPTPath.TrimEnd('\'))</SPTPath>
    <SPTRuntimePath>$(SPTPath)\SPT</SPTRuntimePath>
  </PropertyGroup>

  <!--
    The 4.1 line keeps a client plugin in client/. It is not part of this
    backport, but the folder can still be sitting there after a branch switch,
    and the SDK glob would compile its generated obj files into this assembly
    and fail on duplicate attributes.
  -->
  <ItemGroup>
    <Compile Remove="client\\" />
    <None Remove="client\\" />
    <EmbeddedResource Remove="client\\" />
  </ItemGroup>

  <Target Name="ValidateSPTPath" BeforeTargets="BeforeBuild">
    <Error Condition="'$(SPTPath)' == ''" Text="SPTPath is not set. e.g. -p:SPTPath=H:\SPT4.1.X" />
    <Error Condition="!Exists('$(SPTRuntimePath)\SPTarkov.Server.Core.dll')" Text="SPTPath ('$(SPTPath)') does not contain SPT\SPTarkov.Server.Core.dll" />
  </Target>

  <ItemGroup>
    <Reference Include="SPTarkov.Server.Core"><HintPath>$(SPTRuntimePath)\SPTarkov.Server.Core.dll</HintPath><Private>false</Private></Reference>
    <Reference Include="SPTarkov.Common"><HintPath>$(SPTRuntimePath)\SPTarkov.Common.dll</HintPath><Private>false</Private></Reference>
    <Reference Include="SPTarkov.DI"><HintPath>$(SPTRuntimePath)\SPTarkov.DI.dll</HintPath><Private>false</Private></Reference>
    <Reference Include="SPTarkov.Reflection"><HintPath>$(SPTRuntimePath)\SPTarkov.Reflection.dll</HintPath><Private>false</Private></Reference>
    <Reference Include="SemanticVersioning"><HintPath>$(SPTRuntimePath)\SemanticVersioning.dll</HintPath><Private>false</Private></Reference>
  </ItemGroup>

  <ItemGroup>
    <None Update="config\config.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <Target Name="DeployToSPT" AfterTargets="Build" Condition="'$(DeployToSPT)' == 'true'">
    <PropertyGroup>
      <ModDir>$(SPTPath)\SPT\user\mods\RealisticInsurance</ModDir>
    </PropertyGroup>
    <MakeDir Directories="$(ModDir)\config" />
    <Copy SourceFiles="$(TargetPath)" DestinationFolder="$(ModDir)" />
    <Copy SourceFiles="config\config.json" DestinationFolder="$(ModDir)\config" />
  </Target>

  <!--
    Drop-in archive. Root is SPT\ so the zip extracts straight over the
    SPT install folder (4.1 renamed SPT\ to SPT\).
      dotnet build -c Release -p:SPTPath="H:\SPT4.1.X" -t:PackageRelease
  -->
  <Target Name="PackageRelease" DependsOnTargets="Build">
    <PropertyGroup>
      <StagingDir>$(MSBuildProjectDirectory)\dist\staging</StagingDir>
      <ModStageDir>$(StagingDir)\SPT\user\mods\RealisticInsurance</ModStageDir>
      <ZipPath>$(MSBuildProjectDirectory)\dist\RealisticInsurance-$(Version)-SPT4.0.zip</ZipPath>
    </PropertyGroup>
    <RemoveDir Directories="$(StagingDir)" />
    <MakeDir Directories="$(ModStageDir)\config" />
    <Copy SourceFiles="$(TargetPath)" DestinationFolder="$(ModStageDir)" />
    <Copy SourceFiles="config\config.json" DestinationFolder="$(ModStageDir)\config" />
    <Copy SourceFiles="README.md" DestinationFolder="$(ModStageDir)" />
    <Copy SourceFiles="LICENSE" DestinationFolder="$(ModStageDir)" />
    <Delete Files="$(ZipPath)" />
    <ZipDirectory SourceDirectory="$(StagingDir)" DestinationFile="$(ZipPath)" />
    <RemoveDir Directories="$(StagingDir)" />
    <Message Importance="high" Text="Release archive: $(ZipPath)" />
  </Target>

</Project>
